# PdfEditor 3.3 (Breeze333)

3.2 에서 신고된 문제들을 고쳤습니다. **그린 대로 저장되고, 건 대로 잠기고,
서명은 열자마자 알려 주고, 몇 줄 고쳤다고 파일이 부풀지 않습니다.**

---

## 주석 — 그린 대로 저장됩니다

### 그은 것보다 굵게 나오던 선

밑줄·취소선·연필·직선·화살표·도형을 그은 뒤 **"변경사항 PDF 적용"** 을 누르면
선이 설정한 굵기보다 굵어졌습니다.

화면은 페이지를 2배로 키워 보여 주는데, **도형 크기만 2배 하고 선 굵기는 그대로**
두고 있었습니다. 그래서 그리는 동안에는 절반 굵기로 보였고, 적용하면 원래
굵기로 돌아온 것입니다. 이제 미리보기가 실제 굵기 그대로 보입니다.

### 감당 못 하게 커지던 화살촉

PDF 의 화살촉은 **크기를 지정하는 방법이 없습니다.** 뷰어가 선 굵기에 비례해
그리는데, 그 비율이 선 굵기의 10배가 넘습니다. 굵기 8이면 화살촉 하나가
82pt 짜리 삼각형이 됩니다. 굵기 1.5만 넘어도 감당이 안 됐습니다.

이제 **화살촉을 프로그램이 직접 그립니다.** 굵기에 따라 커지되 훨씬 완만합니다.

| 선 굵기 | 예전 | 지금 |
|---:|---:|---:|
| 1.5 | 15 × 16 pt | 9 × 9 pt |
| 4.0 | 41 × 42 pt | 13 × 15 pt |
| 8.0 | 82 × 84 pt | 26 × 30 pt |

화살촉이 선 길이의 절반을 넘지 않게 상한도 뒀습니다. 뷰어마다 다르게 보이던
것도 없어집니다. 실선·점선·파선·중파선·물결·열린 화살촉·양방향 **전부** 같습니다.

### 점선이 실선으로 저장되던 문제

**점선·파선·중파선을 골라도 저장하면 전부 실선이 됐습니다.** 화살표뿐 아니라
밑줄·취소선·도형·직선도 마찬가지였습니다. 원인이 두 개 겹쳐 있었습니다.

하나는 PDF 라이브러리가 소수점이 든 간격값을 조용히 버리고 실선으로 만드는
것이었고, 다른 하나는 간격이 선 굵기와 무관한 고정값이라 굵은 선에서는
둥근 획 끝이 간격을 메워 버리는 것이었습니다.

간격을 **선 굵기에 비례**하게 바꿨습니다. 굵은 선에는 굵은 선에 맞는 간격이
들어갑니다.

### 자로 그은 것 같던 연필

마우스는 빠르게 그으면 좌표를 띄엄띄엄 줍니다. 그 점들을 직선으로 이어서
울퉁불퉁했습니다. 이제 점 사이를 곡선으로 채웁니다. 획이 꺾이는 각도가
**27.5도에서 0.9도로** 줄었습니다.

화면만 매끄럽게 한 것이 아니라 저장되는 점 목록 자체를 다듬었습니다.
그리는 동안 매끄럽다가 적용하면 각지는 일이 없습니다.

---

## 암호와 권한 — 이제 진짜로 잠깁니다

### 열람 암호를 걸었는데 모든 권한이 열려 있던 문제

권한 암호를 비워 두면 **열람 암호를 그대로 권한 암호로** 쓰고 있었습니다.
그러면 문서를 연 사람이 곧 권한 소유자가 되어 **체크해 둔 제한이 전부
풀렸습니다.**

- 두 암호가 같으면 알려 주고 다시 받습니다.
- 권한 암호를 비운 채 제한을 걸면, 아무도 모르는 권한 암호를 자동으로
  만들어 걸지 물어봅니다.

### 열람 암호만 알면 제한을 통째로 풀 수 있던 문제

더 큰 구멍이 있었습니다. 열람 암호로 열어서 **그냥 저장하면 암호도 권한도
없는 사본**이 만들어졌습니다. 편집을 막아 놔도 아무 의미가 없었습니다.

열람 암호로만 연 문서에서는 다음을 전부 막습니다.

- 저장 / 다른 이름으로 저장
- 내보내기 (Markdown, PNG·JPEG, 플래튼 이미지 PDF, FDF, XFDF)
- 용량 줄이기, 분할
- **자동 저장** — 자동저장 폴더에 암호가 풀린 사본이 쌓이고 있었습니다

권한 암호로 열면 소유자이므로 전부 그대로 됩니다.

### 권한 제한 문서에서 캡처가 되던 문제

복사가 금지된 문서인데 스냅샷과 영역 캡처가 그대로 됐습니다. 화면을 그림으로
떠서 저장하면 복사 제한이 무의미합니다. 이제 둘 다 막습니다.

### 편집한 것도 아닌데 도구가 먹히던 문제

권한 제한이 걸리면 도구 버튼은 꺼졌지만, **이미 골라 둔 도구나 단축키(P·H·U)로는
계속 그려졌습니다.** 이제 캔버스에서 직접 막습니다.

### 저장하면 암호가 사라지던 문제

저장은 내용을 다시 쓰는 작업이라 암호가 유지되지 않습니다. 원래 권한 암호를
알 수 없으니 프로그램이 되살릴 수도 없습니다. 이제 **저장 전에 알려 주고
확인받습니다.** 모르고 저장했다가 보호가 풀린 파일이 남는 일은 없습니다.

---

## 서명 — 열자마자 알려 줍니다

지금까지는 보안 메뉴에서 "서명 확인"을 눌러야 알 수 있었습니다. 도장이 찍혀
있지 않으면 서명된 문서인지조차 몰랐습니다.

이제 **서명이 든 문서를 열면 알림창이 먼저 뜹니다.**

- 서명자 이름, **소속**, 서명 날짜·시간, 사유, 위치
- 맨 마지막 한 줄로 결론:
  - 파란색 — **서명 후 문서가 변경되지 않았습니다. 원본입니다.**
  - 붉은색 — **서명 후 문서가 변경되었습니다. 변조됨.**

도장을 넣지 않아도 이 알림만으로 바로 알 수 있습니다. 메뉴의 "서명 확인"은
이 알림창을 다시 띄우는 용도입니다.

### 본문을 가리던 서명 도장

도장 크기가 고정되어 있어서 위치에 따라 본문 글자를 덮어 버렸습니다.

**크기(40~400pt)와 투명도(10~100%)** 를 넣었습니다. 투명도를 낮추면 도장 아래
본문이 비쳐 보입니다. 도장에 **한글 이름·사유·위치가 제대로 나옵니다**
(예전에는 서명 라이브러리 기본 도장이라 한글이 나오지 않았습니다).

**전자 서명 삽입** 에도 투명도를 넣었습니다.

### 서명하면 권한이 사라지는 것

서명도 문서를 다시 쓰는 작업이라 암호·권한이 유지되지 않습니다. 서명된 파일을
그대로 배포하면 누구나 편집할 수 있게 됩니다. 서명 전에 알려 줍니다.

> **암호와 디지털 서명은 함께 쓸 수 없습니다.** 서명한 뒤 암호를 걸면 서명이
> 무효가 되고, 암호를 건 뒤 서명하면 암호가 풀립니다. PDF 구조상의 제약입니다.
> 원본 보존·진본 증명이 목적이면 서명을, 열람·편집 통제가 목적이면 암호를
> 쓰시고, 둘 다 필요하면 서명본은 보관용으로 두고 배포본만 암호화하세요.

---

## 저장

### 몇 줄 고쳤는데 파일이 6배로 커지던 문제

본문 편집에 시스템 글꼴을 쓰면 **글꼴 파일이 통째로** PDF 에 들어갔습니다.
한글 글꼴은 7MB 가 넘습니다. 글자 몇 개를 넣든 파일 전체가 들어갑니다.

이제 저장할 때 **실제로 쓴 글자만 남깁니다.** PDF 표준 방식이고 아크로뱃도
같습니다.

```
1.46MB 문서 + 한글 한 줄  →  8.61MB (예전)  →  1.52MB (지금)
```

문서에 원래 들어 있던 글꼴도 함께 정리되므로 **파일이 원본보다 작아지기도
합니다.** 보이는 것은 아무것도 달라지지 않습니다 — 렌더 결과가 픽셀 단위로
동일한 것을 확인했습니다.

**환경설정 → 저장 옵션**에서 끌 수 있습니다(기본 켬). 이 문서를 글꼴이 설치되지
않은 컴퓨터의 다른 프로그램에서 새 글자를 타이핑해 편집할 일이 있다면 끄세요.

### 저장하면 가끔 내용이 사라지던 문제

원인을 한 가지로 특정하지는 못했습니다. 대신 **결과 자체를 막았습니다.**

임시 파일을 먼저 쓰고, 그 파일을 **다시 열어 페이지 수가 맞는지, 첫 쪽과
마지막 쪽이 실제로 읽히는지 확인한 뒤에만** 원본을 바꿔치웁니다. 확인에
걸리면 저장을 멈추고 알려 주며, **원본 파일은 건드리지 않습니다.**

같은 파일에 저장할 때 문서가 그 파일을 읽는 도중에 교체될 수 있던 지점도
함께 정리했습니다.

### 암호화 사본을 원래 이름으로 저장하면 실패하던 문제

`save to original must be incremental` 오류가 났습니다. 임시 파일을 거쳐
저장하도록 바꿨습니다.

---

## 편집

### 본문 글자 편집도 되돌리기(Ctrl+Z)

본문 편집은 **되돌리기가 아예 되지 않았습니다.** 주석·페이지 삽입/삭제/이동/
회전/자르기만 기록되고 있었습니다.

이제 고치기 직전 페이지를 통째로 기억해 두었다가 Ctrl+Z 때 되돌립니다.
연속으로 고쳤으면 누른 만큼 차례로 되돌아갑니다. 그 페이지에 달아 둔 주석도
그대로 남습니다.

---

## 그 밖에

`logs/` 에 `crash_*.log` 가 자주 쌓여 다운된 것처럼 보였지만, 실제로는 앱이
멀쩡히 돌아가는 중이었습니다. 윈도우가 정상 동작 중에 던졌다가 스스로
처리하는 오류(드래그&드롭, 인쇄 대화상자 등)까지 함께 적히고 있었고, 0바이트
파일도 그대로 남았습니다.

이름을 `fault_*` 로 바꾸고, **아무것도 적히지 않은 파일은 종료할 때 지웁니다.**
이제 파일이 남아 있으면 정말 무언가 기록된 것입니다.

---

*본문 글자 편집과 전자 서명의 큰 틀은 3.1 에서 만든 것 그대로입니다.*
