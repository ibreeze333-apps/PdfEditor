 PDF 편집기 사용설명서

 Breeze333 PDF Editor · 초보자를 위한 완전 가이드

---------------------------------------------------

 목차

1. [설치 방법]
2. [프로그램 시작]
3. [파일 열기·저장]
4. [어노테이션 도구 사용법]
5. [텍스트 편집]
6. [OCR (글자 인식)]
7. [페이지 관리]
8. [PDF 유틸리티]
9. [계산기]
10. [단축키 모음]
11. [자동 저장 및 복구]
12. [주의사항 및 자주 묻는 질문]

----------------------------------------------------

 1. 설치 방법

 
PDFEditor_배포폴더\
    PDFEditor.exe   ← 이것만 더블클릭하면 됩니다


> ⚠️ 폴더째로 유지해야 합니다. `PDFEditor.exe` 파일만 따로 꺼내면 실행되지 않습니다.

------------------------------------------------------------

 
 2. 프로그램 시작

`PDFEditor.exe`를 더블클릭하면 프로그램이 열립니다.

처음 실행 시 빈 화면이 뜨면 정상입니다.
이전에 작업하다 비정상 종료된 파일이 있으면 복구 여부를 묻는 창이 뜹니다 → "복구" 클릭 시 이전 작업 내용 불러옴.

---------------------------------------------------------------

 4. 파일 열기·저장

 PDF 열기

- 메뉴 → `파일` → `열기` (또는 `Ctrl+O`)
- 지원 형식: `.pdf`, `.jpg`, `.jpeg`, `.png`, `.bmp`, `.webp`, `.tiff`

> 💡 이미지 파일을 열면 자동으로 PDF로 변환하여 편집 가능합니다.

 저장

| 저장 | `Ctrl+S` | 현재 파일에 덮어쓰기 |
| 다른 이름으로 저장 | `Ctrl+Shift+S` | 새 파일명으로 저장 |

> ⚠️ 저장 전에 반드시 "PDF에 적용" 버튼을 누르거나 `Ctrl+Shift+A`를 눌러야 어노테이션이 PDF에 실제로 반영됩니다. 그냥 `Ctrl+S`만 하면 임시 주석이 저장되지 않을 수 있습니다.

 내보내기

`파일` → `변환/내보내기` 서브메뉴:

| Word(.docx) 내보내기 | PDF의 텍스트를 Word 파일로 변환 |
| Markdown(.md) 내보내기 | 텍스트를 마크다운으로 변환 |
| 이미지→PDF 변환 | 이미지 파일들을 PDF로 묶기 |
| PNG/JPEG 내보내기 | 각 페이지를 이미지로 저장 |

-------------------------------------------------------------------------------

 5. 어노테이션 도구 사용법

> 💡 도구를 선택한 뒤 PDF 위에서 마우스를 드래그하거나 클릭하세요.
> 우클릭 → 색상, 굵기, 투명도 등 스타일 변경 가능.

 도구 목록

| 🖱️ | 선택 | `V` | 이미 찍힌 주석을 클릭하여 선택, 이동, 삭제 |
| 🟡 | 형광펜 | `H` | 텍스트 위에서 드래그하여 색칠 |
| _ | 밑줄 | `U` | 텍스트 아래에 밑줄 표시 |
| ~~S~~ | 취소선 | `K` | 텍스트 위에 가로줄 표시 |
| ✏️ | 연필 | `P` | 자유롭게 선 그리기 |
| ─ | 직선 | `L` | 직선 긋기 (시작점 → 끝점 드래그) |
| → | 화살표 | — | 화살표 (시작점 → 끝점 드래그) |
| □ | 도형 | `S` | 사각형·원·삼각형 그리기 |
| 🔖 | 스탬프 | — | 도장 찍기 (클릭하면 종류 선택창) |
| 🖼️ | 이미지 | `I` | 이미지 삽입 (클릭하면 파일 선택) |
| T | 텍스트 | `T` | 텍스트 삽입 (클릭 위치에 입력창) |
| 📝 | 메모 | `N` | 스티키 메모 (노란 포스트잇) |

---

 형광펜 사용하기

1. 툴바에서 형광펜(🟡) 클릭 (또는 `H` 키)
2. PDF 텍스트 위에서 마우스 좌클릭 누른 채 드래그
3. 손을 떼면 형광펜이 적용됨

색상/투명도 변경: 형광펜 도구 선택 상태에서 우클릭 → 색상 / 불투명도 선택

-----------------------------------------------------------------

 스탬프 사용하기

1. 툴바에서 스탬프(🔖) 클릭
2. PDF 위에서 클릭하면 스탬프 종류 선택 창이 뜸
3. 원하는 스탬프 클릭하면 그 위치에 찍힘

스탬프 이동: 스탬프 도구가 선택된 상태에서 기존 스탬프 위로 커서를 가져가면 십자 커서(✛)로 바뀜 → 드래그로 이동 가능

원번호 스탬프: 스탬프 도구 선택 후 우클릭 → "원번호 모드" 선택 → 클릭할 때마다 ①②③… 자동 증가

----------------------------------------------------------------------

 도형 그리기

1. 도형(□) 도구 클릭 (또는 `S` 키)
2. 우클릭 → 도형 종류 (사각형/원/삼각형) 선택
3. 우클릭 → 채우기 모드 선택:
   - 속 채우기: 색상으로 채움
   - 흰색 채우기: 흰색으로 채움 (배경 가리기)
   - 테두리만: 속 비움
4. PDF 위에서 드래그하여 그리기

----------------------------------------------------------------------------

 연필 도구

- 그리기: 연필 도구 선택 후 드래그
- 지우기: 연필 도구 선택 후 우클릭 → "지우개 모드" → 드래그로 지우기

-----------------------------------------------------------------------------

 미확정 어노테이션이란?

주석을 찍으면 바로 PDF에 저장되는 게 아니라 임시 레이어에 먼저 보관됩니다.

- 화면 우측 하단 상태바에 "미확정 N개" 표시
- PDF에 적용 버튼 (또는 `Ctrl+Shift+A`): 주석을 PDF에 영구 반영
- 취소 버튼 (🗑): 미확정 주석 전부 삭제

> 💡 여러 주석을 다 찍은 뒤 한 번에 "PDF에 적용"하는 것이 효율적입니다.

-----------------------------------------------------------------------------

 주석 실행 취소

`Ctrl+Z` — 마지막 적용한 주석 1개 취소

-----------------------------------------------------------------------------

 6. 텍스트 편집

 인라인 편집 (PDF 본문 글자 수정)

1. PDF 본문에서 수정할 텍스트 위에서 더블클릭
2. 텍스트 입력창이 뜨면 내용 수정
3. Enter: 확정 / Esc: 취소

> ⚠️ 스캔된 PDF(이미지 기반)는 인라인 편집이 안 됩니다. OCR로 텍스트를 추출한 뒤 편집하세요.

--------------------------------------------------------------------------------

 텍스트 블록 편집

`편집` → `PDF 텍스트 수정`

- PDF 내 모든 텍스트 블록을 목록으로 표시
- 원하는 블록 선택 후 내용/글꼴 수정
- 글꼴 자동 감지 기능 포함

------------------------------------------------------------------------------

 텍스트 찾아 바꾸기

`편집` → `텍스트 찾아 교체` (또는 `Ctrl+H`)

| 찾을 내용 | 검색할 텍스트 |
| 바꿀 내용 | 교체할 텍스트 |
| 대소문자 구분 | 영문 대소문자 구분 여부 |
| 글꼴 선택 | 바꿀 텍스트에 적용할 글꼴 |
| 페이지 범위 | 전체 또는 특정 페이지만 |

-----------------------------------------------------------------------------

 텍스트 에디터

`편집` → `텍스트 에디터` (또는 `Ctrl+T`)

독립 텍스트 편집 창으로 자유롭게 텍스트 작성 가능 (줄 번호, 찾기/바꾸기 포함).

----------------------------------------------------------------------------

 7. OCR (글자 인식)

스캔된 PDF나 이미지에서 글자를 인식합니다.

 OCR 패널 열기

`보기` → `OCR 패널` 또는 툴바의 OCR 버튼 클릭 (또는 `Ctrl+Shift+O`)

 사용 방법

1. OCR 패널에서 설정 선택:

   | 엔진 | ⚡ 빠른 추출 (내장 텍스트) / 🔍 정밀 OCR (PaddleOCR) |
   | 언어 | 한국어+영어 / 영어만 / 일본어+영어 / 중국어+영어 |
   | GPU | GPU 사용 (해당 PC에 GPU가 있어야 빠름) |
   | 페이지 | 현재 페이지만 / 전체 / 범위 지정 |

2. OCR 시작 버튼 클릭
3. 인식 완료 후 텍스트 결과가 패널에 표시됨
4. 복사 버튼으로 클립보드에 복사

> 💡 빠른 추출은 텍스트 기반 PDF에, 정밀 OCR은 스캔 이미지에 사용하세요.

> ⚠️ 정밀 OCR 첫 실행 시 AI 모델 다운로드로 인해 시간이 걸릴 수 있습니다.

--------------------------------------------------------------

 8. 페이지 관리

`페이지` 메뉴에서 모든 페이지 작업을 수행합니다.

 페이지 추가

| 빈 페이지 삽입 | 현재 페이지 뒤에 빈 페이지 추가 |
| 이미지를 페이지로 삽입 | 이미지 파일을 새 페이지로 추가 |

 페이지 삭제

`페이지` → `현재 페이지 삭제` — 현재 보고 있는 페이지를 삭제

> ⚠️ 삭제는 되돌리기(`Ctrl+Z`)가 지원되지 않습니다. 신중하게 사용하세요.

 페이지 회전

- `페이지` → `페이지 회전 (시계방향)` — 90도 우회전
- `페이지` → `페이지 회전 (반시계방향)` — 90도 좌회전

 페이지 크롭

`페이지` → `현재 페이지 크롭`

1. 자르기 핸들을 드래그하여 원하는 영역 선택
2. 확인 버튼 클릭

 페이지 이동

- 썸네일 패널에서 원하는 페이지 클릭
- 보기 툴바의 ◀ ▶ 버튼

-----------------------------------------------------------------

 9. PDF 유틸리티

 PDF 용량 줄이기

`파일` → `PDF 유틸리티` → `📦 PDF 용량 줄이기`

| 구조 최적화 | 파일 구조 정리, 이미지 품질 유지 |
| 이미지 재렌더링 | DPI·품질을 낮춰 용량 대폭 축소 |

> 💡 용량을 많이 줄이려면 "이미지 재렌더링"을 선택하고 DPI를 낮추세요 (권장: 150DPI).

-------------------------------------------------------------------------

 PDF 병합

`파일` → `PDF 유틸리티` → `🔗 이미지/PDF 통합 병합`

1. + 버튼으로 파일 추가 (PDF 또는 이미지 혼합 가능)
2. 순서 조정 (위/아래 이동)
3. 병합 버튼 클릭 → 저장 위치 선택

------------------------------------------------------------------------

 PDF 분할

`파일` → `PDF 유틸리티` → `✂ 크기별 PDF 분할`

- 목표 파일 크기(MB)를 입력하면 자동으로 여러 파일로 분할

또는 `페이지` → `PDF 분할`:
- 특정 페이지 범위/개수로 분할

-------------------------------------------------------------------------

 워터마크 추가

`구성` → `워터마크 추가`

| 텍스트 | 워터마크 문구 (예: "DRAFT", "비밀") |
| 크기 | 글꼴 크기 |
| 불투명도 | 0~100% (낮을수록 흐릿하게) |
| 각도 | 기울기 (예: 45도) |
| 색상 | 워터마크 색상 |

------------------------------------------------------------------------

 머리글/바닥글

`구성` → `머리글/바닥글`

- 페이지 번호, 날짜, 텍스트를 상단/하단에 추가
- 위치: 좌/중/우 선택 가능

--------------------------------------------------------------------

 10. 계산기

`계산기` 메뉴에서 여러 계산기를 사용할 수 있습니다.

| 🧮 기본 계산기 | 사칙연산, 기본 기능 |
| 📐 공학 계산기 | 삼각함수, 로그, 지수 등 |
| 🏠 생활 계산기 | 면적, 단위 변환 등 생활 계산 |
| 💰 금융 계산기 | 이자, 대출 상환 등 |
| ⚖ 법률 계산기 | 8가지 법률 전문 계산 |

 법률 계산기 세부 항목


| 📅 날짜 계산 | 기간, 업무일 계산 |
| ⚖ 지연손해금 | 원금·이율·기간으로 손해금 계산 |
| 🧾 소송비용/인지대 | 청구금액 기준 인지대 산출 |
| ⏰ 소멸시효 | 청구권 유형별 시효 기간 |
| 👔 변호사보수 | 법정 기준율 기반 보수 계산 |
| 🔒 가압류 비용 | 보증금 자동 계산 |
| 👶 양육비 | 부모 소득 기반 양육비 산정 |
| 🏠 유류분 | 상속 관계별 유류분 계산 |
| 💰 변제충당 | 이자/원금 배분 계산 |

----------------------------------------------------

 11. 단축키 모음

 파일

| 단축키 | 기능 |
|--------|------|
| `Ctrl+N` | 새 문서 |
| `Ctrl+O` | 파일 열기 |
| `Ctrl+S` | 저장 |
| `Ctrl+Shift+S` | 다른 이름으로 저장 |
| `Ctrl+Q` | 프로그램 종료 |

 편집

| 단축키 | 기능 |
|--------|------|
| `Ctrl+Z` | 실행 취소 |
| `Ctrl+T` | 텍스트 에디터 열기 |
| `Ctrl+H` | 텍스트 찾아 교체 |
| `Ctrl+D` | 사전 창 열기 |

 보기

| 단축키 | 기능 |
|--------|------|
| `Ctrl+0` | 페이지 크기에 맞게 보기 |
| `Ctrl+Shift+W` | 너비에 맞게 보기 |
| `Ctrl+1` | 실제 크기 (100%) |
| `Ctrl+Shift+O` | OCR 패널 열기/닫기 |
| `Ctrl+Shift+C` | 화면 스냅샷 → 클립보드 복사 |
| 마우스 휠 | 확대/축소 |

 어노테이션 도구

| 단축키 | 도구 |
|--------|------|
| `V` | 선택 |
| `H` | 형광펜 |
| `U` | 밑줄 |
| `K` | 취소선 |
| `P` | 연필 |
| `L` | 직선 |
| `S` | 도형 |
| `I` | 이미지 삽입 |
| `T` | 텍스트 삽입 |
| `N` | 메모 |

 어노테이션 적용/취소

| 단축키 | 기능 |
|--------|------|
| `Ctrl+Shift+A` | PDF에 적용 (미확정 → 확정) |
| 툴바 🗑 버튼 | 미확정 주석 전부 취소 |

-------------------------------------------------------------

 12. 자동 저장 및 복구

 자동 저장

- 프로그램이 30초마다 자동으로 임시 저장합니다
- 저장 위치: `C:\Projects\pdf_editor\autosave\autosave.pdf`
- 정상 종료 시 자동 저장 파일은 자동 삭제됩니다

 비정상 종료 후 복구

프로그램이 갑자기 꺼진 경우:

1. 프로그램 다시 실행
2. "이전 작업을 복구하시겠습니까?" 창이 뜸
3. "복구" 클릭 → 마지막 자동 저장 시점의 작업 내용 불러옴

> 💡 중요한 작업은 자동 저장에 의존하지 말고 `Ctrl+S`로 수시로 저장하세요.

---------------------------------------------------------------

 13. 주의사항 및 자주 묻는 질문

 ⚠️ 꼭 읽어주세요

--------------------------------------------------------------

Q. 주석을 찍었는데 저장하면 사라진다?

A. 주석은 먼저 "PDF에 적용" (`Ctrl+Shift+A`)을 눌러야 합니다.
적용 후 `Ctrl+S`로 저장하세요. 순서가 중요합니다.

---------------------------------------------------------------

Q. 저장했는데 파일이 없다?

A. "다른 이름으로 저장"(`Ctrl+Shift+S`)으로 저장하면 저장 위치를 직접 선택합니다.
`Ctrl+S`(그냥 저장)는 원본 파일 위치에 덮어쓰기합니다.

-----------------------------------------------------------------

Q. 텍스트를 수정했는데 글자가 깨진다?

A. PDF에 포함된 글꼴이 다를 때 발생합니다.
텍스트 삽입 시 `KoPub` 또는 `Malgun Gothic` 계열 글꼴을 선택해 보세요.

-------------------------------------------------------------

Q. OCR이 너무 느리다?

A. 처음 실행 시 AI 모델을 다운로드하기 때문에 느릴 수 있습니다.
두 번째 실행부터는 빨라집니다.
GPU가 있다면 OCR 패널에서 "GPU 사용"을 체크하면 빨라집니다.

------------------------------------------------------------

Q. 이미지로만 된 PDF를 편집하고 싶다?

A. OCR을 먼저 실행하여 텍스트를 인식한 후, 인식된 텍스트를 편집하세요.
스캔본 자체의 픽셀을 수정하는 것은 지원하지 않습니다.




------------------------------------------------------------

Q. 실행 취소가 안 된다?

A. `Ctrl+Z`는 "PDF에 적용"한 마지막 주석 1개만 취소됩니다.
미확정 상태(적용 전)의 주석은 툴바의 🗑 버튼으로 전부 취소하세요.

------------------------------------------------------------

Q. PDF 용량이 너무 커졌다?

A. `파일` → `PDF 유틸리티` → `PDF 용량 줄이기`를 사용하세요.
이미지 재렌더링 방식에서 DPI를 150으로 설정하면 용량이 크게 줄어듭니다.

------------------------------------------------------------

Q. 프로그램이 갑자기 꺼진다?

A. 로그 파일을 확인하세요: `C:\Projects\pdf_editor\logs\pdf_editor_날짜.log`
제작자에게 해당 파일을 전달하면 원인 파악에 도움이 됩니다.

------------------------------------------------------------

Q. 특정 PDF가 열리지 않는다?

A. 암호화된 PDF는 지원하지 않습니다.
또한 매우 오래된 PDF 버전(1.3 이하)은 일부 기능이 제한될 수 있습니다.

------------------------------------------------------------

Q. 배포판 폴더를 다른 위치로 옮겨도 되나?

A. 네, 가능합니다. 단, 폴더째로 이동해야 합니다.
`PDFEditor.exe` 파일만 따로 꺼내면 실행되지 않습니다.

------------------------------------------------------------

Q. 설정 초기화 방법은?

A. 탐색기에서 `C:\Users\[사용자명]\.pdf_editor_settings.json` 파일을 삭제하면
다음 실행 시 설정이 초기화됩니다.

------------------------------------------------------------

 지원 글꼴 목록

| 글꼴 | 사용 가능 위치 |

| KoPub Batang Light/Medium/Bold | 텍스트 삽입, 텍스트 블록 편집 |
| KoPub Dotum Light/Medium/Bold | 텍스트 삽입, 텍스트 블록 편집 |
| Malgun Gothic / Bold | 텍스트 삽입, 찾아 바꾸기 |
| HANBatang / Bold | 텍스트 삽입 |
| 굴림 / 바탕 / 돋움 | 시스템 글꼴 |
| Times New Roman / Arial | 영문 글꼴 |

------------------------------------------------------------
 시스템 요구사항

| 항목 | 최소 | 권장 |
| OS | Windows 10 64bit | Windows 11 64bit |
| RAM | 4GB | 8GB 이상 |
| 디스크 | 2GB 여유 공간 | 5GB 이상 |
| GPU | 없어도 됨 | NVIDIA GPU (OCR 가속) |
| 화면 해상도 | 1280×768 | 1920×1080 이상 |

---------------------------------------------------------

*이 문서는 Breeze333 PDF Editor v1.0 기준으로 작성되었습니다.*
