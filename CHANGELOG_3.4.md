# PdfEditor 3.4 (Breeze333)

**찾기가 제대로 됩니다.** 편집 메뉴 구석에 있던 찾기를 `찾기(D)` 메뉴로 따로
꺼냈고, 열리기만 하고 찾아지지 않던 문제를 고쳤습니다. 그 밖에 화면 배율이 높은
노트북에서 버튼이 잘리던 문제, 실패하면 열어 둔 문서까지 날아가던 문제 등을
고쳤습니다.

---

## 찾기 — 이제 찾아집니다

### 찾기 메뉴

| 항목 | 단축키 |
|---|---|
| 찾기... | Ctrl+F |
| 다음 찾기 / 이전 찾기 | F3 / Shift+F3 |
| 대소문자 구분, 단어 단위로 | (체크) |
| 고급 검색 (결과 목록)... | Ctrl+Shift+F |
| 검색 강조 지우기 | |
| 텍스트 찾기/바꾸기 | Ctrl+H |

`텍스트 찾기/바꾸기`도 편집 메뉴에서 찾기 메뉴로 옮겼습니다.

### 찾기 막대가 열리기만 하고 아무것도 안 되던 문제

원인이 세 개 겹쳐 있었습니다.

- **버튼이 안 보였습니다.** 이전·다음·닫기 버튼 크기를 작게 고정해 뒀는데, 앱
  전체 스타일이 버튼 최소 높이를 더 크게 잡았습니다. 버튼이 막대보다 커져서
  막대 밖으로 잘려 나갔습니다.
- **찾아도 엉뚱한 곳이 칠해졌습니다.** 강조 상자 위치 계산에 화면 배율을 잘못
  곱해서, 배율 100% 에서는 단어의 절반 위치에 절반 크기로 그려졌습니다. 찾은
  글자 위에는 아무것도 없으니 결과가 없는 것처럼 보였습니다.
- **큰 문서에서 멈췄습니다.** 글자를 하나 칠 때마다 모든 페이지를 한꺼번에
  뒤져서, 수백 쪽짜리 문서에서는 입력할 때마다 화면이 몇 초씩 굳었습니다.

### 새 찾기 막대

문서 오른쪽 위에 뜹니다. 입력하는 대로 찾고, 찾는 동안에도 계속 입력할 수
있습니다.

- 찾은 곳을 **모두 옅게**, 지금 보고 있는 결과는 **진하게** 칠합니다.
- 결과가 화면 밖에 있으면 그 자리로 스크롤합니다.
- 한글을 조합하는 중인 글자까지 바로 찾습니다.
- 검색어 기록이 남습니다(▾).
- **스캔본**(글자가 없는 이미지 PDF)이면 "결과 없음 · 스캔본"으로 알려 줍니다.
  OCR 을 돌린 뒤에는 인식한 글자에서 찾습니다. 다만 화면상 위치 정보가 없어
  페이지로만 이동합니다.

### 고급 검색 — 결과를 목록으로

`찾기 → 고급 검색`(Ctrl+Shift+F)은 오른쪽에 **검색** 패널을 엽니다.

- **검색 방식**: 정확한 문구 / 모든 단어 포함 / 하나라도 포함
- **제외할 단어**: 이 단어가 있는 페이지는 결과에서 뺍니다
- **범위**: 현재 문서 / 열린 모든 문서
- 결과마다 **쪽 번호와 앞뒤 문장**이 나오고, 누르면 그 자리로 이동합니다.

---

## 화면 — 배율 높은 노트북에서도 다 보입니다

윈도우 화면 배율을 올리면(예: 175%) 오른쪽 끝 버튼들이 잘려서 안 보이고, 버튼
글씨가 조금씩 틀어져 보였습니다. 이제 폭이 모자라면 **글씨를 필요한 만큼만**
줄여 한 줄에 맞추고, 그래도 안 되면 다음 줄로 넘깁니다.

`변경사항 PDF에 적용` → `PDF에 적용`, `적용 취소` → `취소` 로 버튼 이름을
줄였습니다(설명은 마우스를 올리면 나옵니다).

### 메뉴 버튼 스타일

`보기 → 메뉴 버튼 스타일`에서 **Liquid Glass**(기본)와 **기존 스타일**을 바로
바꿀 수 있습니다. 고른 스타일은 다음 실행에도 유지됩니다.

### 새 아이콘

프로그램 아이콘을 바꿨습니다.

---

## 안정성 — 실패해도 열어 둔 문서는 그대로입니다

| 이런 때 | 이제는 |
|---|---|
| 다른 파일을 열다 실패하거나 PDF 암호를 틀림 | 열려 있던 문서와 저장 안 한 변경이 그대로 남습니다 |
| 암호로 연 제한 문서 다음에 다른 문서를 엶 | 이전 문서의 암호·편집 제한이 따라오지 않습니다 |
| 다른 탭에 저장 안 한 변경이 있는 채 종료 | 탭마다 저장/버리기/취소를 묻습니다 |
| 주석 저장 확인창을 X 로 닫음 | 저장을 취소합니다(주석을 빼고 저장하지 않습니다) |
| Ctrl+W | 탭이 실제로 닫힙니다 |
| Ctrl+Shift+A | PDF에 적용만 합니다. Ollama 분석은 Ctrl+Alt+A 입니다 |
| 설정 파일이 깨짐 | 깨진 항목만 기본값으로 되돌리고 시작합니다 |
| 서명 결과를 원본과 같은 이름으로 저장 | 원본을 먼저 지우지 않고, 완성된 뒤에 바꿔 끼웁니다 |
| 크기별 분할을 같은 폴더에 다시 실행 | 덮어쓰기·다른 이름·취소를 묻습니다 |

---

## 보안 — 위장 파일 차단을 보강했습니다

확장자를 `.pdf` 로 바꾼 실행 파일을 여는 것을 막는 검사가 **절반은 동작하지
않았습니다.** 배치 파일·PowerShell·JavaScript·레지스트리 파일·JAR·APK·바로가기를
`.pdf` 로 위장하면 그냥 통과했습니다. 이제 모두 막습니다.

---

## 배포본 — 가벼워졌습니다

같은 기능 그대로 배포본 용량이 **약 690 MB → 약 440 MB** 로 줄었습니다.
OCR 엔진은 검증된 5.5.0 을 그대로 쓰며, 인식 결과는 이전과 같습니다.
