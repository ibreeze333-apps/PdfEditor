# PDF Editor 2.5 변경점

2.5는 성능·안정성·보안을 집중적으로 손본 버전입니다. 기능 추가보다 "가볍고
오류 없이 도는 것"에 초점을 맞췄습니다.

## 2.5 재발행 추가분 (번역 기능)

- **번역 허브 신설** (번역 메뉴 · 툴바 🌐 버튼 · Ctrl+Shift+H)
  - 🦙 Ollama 로컬 번역 (페이지/직접 입력, 스캔본은 OCR 후 번역)
  - 🔑 API 번역: ChatGPT·Claude 프리셋 + OpenAI 호환 서비스(Groq,
    DeepSeek 등) 사용자 추가 — API 키는 사용자가 직접 입력하며
    Windows 계정 키로 암호화(DPAPI)되어 저장
  - 🌐 웹 AI: 프롬프트+원문 복사 후 ChatGPT/Claude/DeepL 등 브라우저 열기
- **Ollama 분석 창에 번역 탭 추가** — 요약·Q&A 등과 같은 방식으로 사용
- **온라인 롤오버 사전**: 단어에 마우스를 올리면 네이버사전 뜻이 팝업에
  바로 표시 (영↔한, 캐시 지원) — 버튼을 눌러 검색하던 방식 개선
- 스크롤 보기에서 첫 페이지 이외에는 롤오버 사전·hover 툴팁·드래그
  선택이 동작하지 않던 버그 수정
- 툴바 토글 버튼의 켜짐 상태를 반전 스타일로 표시 — 어떤 기능이
  활성인지 한눈에 구분
- 환경설정에 '기본값으로 초기화' 버튼 추가 (사전 경로·API 키는 유지)

## 성능

- **시작 시간 4.8초 → 1.0초**
  - 줌 컨트롤에 쓰인 `−`(U+2212) 글자가 기본 폰트에 없어 첫 표시 때 시스템
    폰트 전체를 훑던 문제(2.9초) — ASCII `-`로 교체
  - 시작 시 보이지도 않는 텍스트 초안 편집기·읽기 뷰가 글꼴 목록을 미리
    채우던 것(1.7초) — 실제로 열 때 생성하도록 변경
  - 사전(17.8만 단어) 로딩(1.9초) — 창이 뜬 뒤 백그라운드로 이동
- **대용량 PDF**: 115MB / 150페이지 기준 열기 0.02초, 메모리 +97MB.
  문서를 통째로 메모리에 올리지 않고 필요한 페이지만 읽습니다.
- 렌더 캐시에 256MB 메모리 상한 추가
- 어노테이션 여러 개를 한 번에 반영할 때 문서 직렬화가 매번 일어나던 것을
  1회로 축소

## 안정성 (버그 수정)

- **저장 시 문서가 손상되던 문제**: 같은 경로에 저장하면 열려 있는 문서의
  원본 파일을 덮어써서, 저장 직후 텍스트가 모두 빈 값이 되고 화면이 깨졌습니다.
  이제 임시 파일에 완전히 기록한 뒤 원자적으로 교체합니다 — 저장 중 실패해도
  원본이 남습니다.
- **긴 문서 → 짧은 문서를 열면 오류가 쏟아지던 문제**: 이전 문서의 페이지
  번호가 남아 마우스를 움직일 때마다 오류 창이 반복 표시됐습니다.
- **탭을 닫아도 내용이 남던 문제**: 제목만 바뀌고 페이지는 그대로 보이다가
  페이지를 넘겨야 사라졌습니다.
- 페이지 삭제·회전 후 썸네일이 어긋나던 문제

## 보안

- **위장 파일 검사가 배포판에서 동작하지 않던 문제 수정**: 빌드에서 추론
  런타임이 빠져 있어 검사가 조용히 실패하고 모든 파일이 통과했습니다.
  이제 `.pdf`로 위장한 실행파일(EXE/MSI/스크립트 등)을 정상 차단합니다.
- **검사 우회 경로 차단**: 파일 메뉴로 열 때만 검사하고 드래그앤드롭이나
  파일 연결·명령줄로 열면 그대로 통과하던 구멍을 막았습니다.
- 검사 실패 시 조용히 넘어가지 않고 로그에 경고를 남깁니다.

## 편의

- **확대 크기 % 표시**: 스냅샷 창 버튼 앞에 현재 배율을 실시간 표시
- **스캔본 확대 시 선명도 개선**: 텍스트가 없는 이미지 페이지도 확대하면
  배율에 맞춰 고화질로 다시 그립니다 (기본 배율에서는 기존대로 — 메모리 절약)
- **되돌리기 확장**: 어노테이션뿐 아니라 페이지 회전·자르기·삭제·삽입·이동도
  Ctrl+Z로 되돌립니다 (삭제한 페이지는 내용까지 복원)

## 배포

- **설치 용량 1504MB → 472MB**
  - 앱이 전혀 사용하지 않는 OpenCV(133MB) 제외
  - GPU 전용 추론 라이브러리와 그 의존 CUDA 라이브러리(1,145MB) 제외 —
    파일 검사는 CPU만 사용합니다
  - Tesseract 중복 언어 데이터·Java 도구 제외(15MB)
- 빌드 스크립트가 실행하면 무조건 실패하던 버그 수정
- 로그가 수백 MB까지 쌓이던 문제 수정 (기록 수준 조정 + 14일 지난 로그 자동 삭제)
