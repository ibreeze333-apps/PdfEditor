# PdfEditor 2.6 (Breeze333)

2.5 이후 추가·개선된 내용입니다.

## 보안 (신규)

상단에 **보안** 메뉴가 생겼습니다. 모두 PDF 표준을 따르므로 다른 PDF 앱에서도
똑같이 동작합니다.

- **열람 암호 / 권한 암호**: AES-256으로 저장. 열람 암호는 파일을 열 때,
  권한 암호는 인쇄·복사·편집 등 6가지 권한을 따로 제한합니다. 두 암호를
  다르게 걸면 실제로 다르게 동작합니다(앱도 제한을 지킵니다).
- **전자 서명(표준 인증서)**: 보유한 `.p12`/`.pfx` 인증서로 서명하거나, 없으면
  자체 서명 인증서를 만들어 서명합니다. 다른 앱에서도 서명이 검증됩니다.
  서명 위치·사유·장소를 지정할 수 있습니다.
- **개인정보 교정(redaction)**: 주민등록번호·전화번호·이메일·카드번호·계좌번호를
  자동으로 찾아 지웁니다. 직접 입력한 문구도 지울 수 있습니다.
- **워터마크**: 문서 전체에 텍스트/이미지 워터마크를 넣습니다.
- 암호가 걸린 PDF를 열고, 모든 보기 모드에서 정상적으로 볼 수 있습니다.
- API 키를 **Windows DPAPI로 암호화**해 저장합니다(로그인 계정에서만 복호화).

## 본문 글자 편집 (대폭 개선)

유료 앱처럼 **문서 위에서 바로** 글자를 고칠 수 있습니다.

- **문단 단위 제자리 편집**: 마우스를 올리면 편집할 문단에 사각형이 표시되고,
  클릭하면 그 자리에서 문단 전체를 워드처럼 고칩니다.
- **서식 툴바**: 글꼴 · 크기 · 굵게/기울임/밑줄 · 글자색 · 정렬.
- **한글 조판 PDF 대응**: 한글 학술지·단행본 PDF는 단어 사이 공백이 저장되지
  않아 글자가 붙거나 순서가 뒤엉기던 문제를, 글자 위치를 읽어 **사람이 보는
  순서·간격 그대로** 복원하도록 고쳤습니다.
- **배치 보존 저장**: 고친 줄만 원래 자리에 되돌려 놓습니다. 각주의 내어쓰기,
  문단 첫 줄 들여쓰기, 줄간격, 양쪽 정렬이 그대로 유지됩니다.
- **문단 다시 흘리기(캔바식) 토글**: 켜면 문단이 하나의 흐르는 글이 되어
  자유롭게 줄바꿈됩니다. 글을 많이 넣어도 자연스럽게 재배치됩니다.
- 고친 줄이 문단·상자 밖으로 삐져나가지 않게 여백 안에 맞춥니다.
- 각주의 볼드 제목이 사라지던 문제, 가운뎃점(·)과 움라우트(ü)·대시(—)가
  □로 깨지던 문제를 고쳤습니다.

## 메모 · 인덱스

- **인덱스 탭**을 PDF 표준 책갈피 기반으로 다시 만들었습니다. 모든 페이지에서
  보이고, 클릭하면 해당 페이지로 이동합니다. 좌우 양쪽에 붙일 수 있고
  위아래로 자유롭게 옮길 수 있으며, 실제 종이 같은 모양입니다.
- **인덱스 표시 필터**: 우상단 버튼으로 「전체 → 내 인덱스만 → 문서 목차만」
  순환. 책갈피가 많은 PDF에서 문서 목차는 회색으로 흐리게, 내가 만든 인덱스는
  지정 색으로 도드라지게 표시합니다. 버튼 우클릭으로 목차/내 인덱스를 좌·우로
  한꺼번에 옮길 수 있습니다.
- 인덱스가 화면 높이를 넘치면 휠로 스크롤됩니다(▲▼ 표시). 책보기에서는 인덱스
  바 높이를 페이지에 맞춰 삐져나가지 않게 했습니다.
- 인덱스를 추가·저장해도 PDF 원래 목차의 목적지(장 제목 위치)를 그대로 보존해,
  다른 PDF 뷰어에서도 목차 이동이 정확합니다.
- **메모**: 모서리로 크기 조절, 투명도, 글꼴 선택, 글자에 맞춘 자동 크기 조절.
  메모를 옮길 때 앱이 꺼지던 문제를 고쳤습니다.

## 번역 · 사전

- **번역 허브**: Ollama(로컬) · API(ChatGPT/Claude/커스텀) · 웹 AI를 한 곳에서.
  API 키는 사용자가 직접 넣습니다(앱에 키가 들어있지 않습니다).
- **온라인 롤오버 사전**: 단어에 마우스를 올리면 뜻이 바로 표시됩니다.

## 화질 · 성능

- 스크롤 보기에서 편집·저장한 뒤 화면이 흐린 채로 남던 문제를 고쳤습니다.
- 스캔 페이지가 고해상도(윈도우 배율 150%·200%) 화면에서 흐리게 그려지던
  문제를 고쳤습니다.
- 대용량 PDF의 버벅임을 줄이고, 멈춤 감지 워치독을 넣었습니다.

## 기타

- 툴바 버튼이 잘리던 문제, 버튼 눌림 표시, 보기 모드 선택 후 포커스 복귀.
- 환경설정에 **기본값으로 초기화** 버튼.
- 탭을 열 때 콘솔에 경고가 쏟아지던 문제를 고쳤습니다.
