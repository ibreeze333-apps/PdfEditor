# PdfEditor 3.1 (Breeze333)

3.0 이후 **본문 글자 편집**과 **전자 서명**을 표준 방식으로 다시 만들었습니다.

## 본문 글자 편집 — 표준 방식으로 재구현

### 옛 글자를 실제로 지웁니다
지금까지는 옛 글자를 흰 사각형으로 가리고 그 위에 새 글자를 그렸습니다. 화면은
멀쩡했지만 파일 속에는 옛 글자가 남아, 같은 자리를 다시 고치면 두 글자가 겹쳐
읽히며 `－서명레을비 나고스쳐의보자관점으로…` 처럼 뒤죽박죽이 됐습니다.

이제 Acrobat이 하는 것처럼 **PDF 내부의 글자 그리기 명령만 정밀하게 지웁니다.**
손대지 않은 글자는 전혀 영향을 받지 않습니다. (지운 뒤 검사해서 조금이라도
이상하면 원래대로 되돌리고 기존 방식으로 안전하게 넘어갑니다.)

### 글이 길어지면 다음 줄로 넘어갑니다
글자 크기를 줄이지 않고 **문단을 다시 흘려 다음 줄로** 넘깁니다(Acrobat과 동일).
크기 축소는 페이지 끝까지 넓혀도 안 들어갈 때만 일어납니다.

### 각주도 자연스럽게
각주(`*`, `**`, `***`)는 항목별로 다시 흘립니다. **별표 위치·내어쓰기·글자
크기가 그대로 유지된 채** 줄만 늘어나고, 아래 항목이 자연스럽게 내려갑니다.

### 그 밖의 편집 개선
- 문단이 조각나 `엠`, `Green` 같은 짧은 줄이 생기던 문제 수정
- 상자 안 문단을 고치면 위로 밀려 올라가던 문제 수정
- `요 약` 같은 제목이 본문과 한 덩어리로 재배치되던 문제 수정
- 가운뎃점(·)·움라우트(ü)·대시(—)가 □로 깨지던 문제 수정
- 각주의 굵은 인용 제목이 사라지던 문제 수정
- **본문편집 모드를 켜지 않으면** 더블클릭해도 편집창이 열리지 않습니다
  (읽다가 실수로 글자가 바뀌는 것 방지)

## 전자 서명

- **서명이 살아남습니다**: 서명된 문서를 고쳐 저장할 때 변경분만 덧붙이는
  방식(증분 저장)을 씁니다. Adobe와 마찬가지로 서명은 유효한 채로 남고
  "서명 이후 변경됨"으로 표시됩니다.
- **서명 확인** 기능이 생겼습니다(보안 메뉴). 서명자·시각·사유와 함께
  위·변조 여부를 알려 줍니다. 자체 서명 인증서는 "내용 보호는 되지만 신원은
  보증되지 않음"으로 정확히 구분합니다.
- 서명란(도장)만 남고 서명 데이터가 없는 문서를 정확히 안내합니다.
- 서명 시 `/Metadata` 오류로 실패하던 문제, 저장 과정에서 서명 목록이 비워지던
  문제를 고쳤습니다.

## 인덱스 · 보기 (3.0 기능 다듬기)

- 인덱스 표시 필터(전체 / 내 인덱스 / 문서 목차 / 숨김)
- 책보기에서 인덱스가 가장자리에 숨었다가 마우스를 대면 부드럽게 나옵니다
- 하단 페이지 슬라이더, 상단 툴바 자동 숨김(F11)

---

3.0 의 인덱스·보기 기능은 CHANGELOG_3.0.md, 2.6 의 보안 메뉴는
CHANGELOG_2.6.md 를 참고하세요.
